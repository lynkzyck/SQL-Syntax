---- Lọc so sánh dữ liệu cùng 1 table

SELECT p1.country_code,
       p1.size AS size2010,
       p2.size AS size2015
FROM populations as p1
JOIN populations as p2
    ON p1.country_code = p2.country_code AND p1.year = p2.year - 5 
    
    
    
